[build-system]
requires = ["maturin==1.14.1"]
build-backend = "maturin"

[project]
name = "star-native"
requires-python = ">=3.10"
dynamic = ["version"]

[tool.maturin]
manifest-path = "Cargo.toml"
module-name = "star_native"

